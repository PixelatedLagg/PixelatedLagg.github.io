var text = document.getElementById("text");

function input(arg) {
    switch (arg)
    {
        case "csharp":
            text.innerHTML = "Pixelated is very skilled in C#. Having used it for about 2 years, they can create basically anything with it."
            break;
    }
}